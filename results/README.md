# Results

`mock/` contains deterministic software-fixture outputs only. Its apparent predictive signal is scripted. Actual API spend is zero. Never combine these records with calibration or main-study data.

Regenerate with `python -m space_canary dry-run` then `python -m space_canary analyze`.

Paid results belong in an ignored `live/` directory until reviewed for release. There are none yet.
